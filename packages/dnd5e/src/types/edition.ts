/** The two D&D 5e rules editions Archivist canonicalizes content for.
 *  Single source of truth — obsidian `class.types` and the dnd5e entity
 *  types both import it from here. */
export type Edition = "2014" | "2024";
